<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie List</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
    <!-- Title -->
    <header class="text-center py-6">
        <h2 class="text-2xl font-bold text-gray-800">Edit Movie List</h2>
        <a href="/" class="text-blue-600 hover:text-green-500 transition-colors">Go to Dashboard</a>
    </header>

    <!-- Movie List -->
    <main class="max-w-4xl mx-auto p-4 space-y-6">
        {{#each Movies}}
        <div class="flex flex-col sm:flex-row items-center bg-white rounded-lg shadow-md overflow-hidden p-4">
            <!-- Movie Poster -->
            <div class="flex-shrink-0 w-24 h-32 sm:w-32 sm:h-40">
                <img src="{{this.image}}" alt="Movie Poster" class="w-full h-full object-cover rounded-md">
            </div>
            <!-- Movie Details -->
            <div class="sm:ml-6 mt-4 sm:mt-0 flex-1">
                <a href="/movies/{{_id}}" class="text-xl font-semibold text-gray-800 hover:text-blue-600 transition-colors">
                    {{this.title}}
                </a>
                <p class="text-sm text-gray-500 mt-1">
                    Release Date: {{this.releaseDate.year}}-{{this.releaseDate.month}}-{{this.releaseDate.day}}
                </p>
                <p class="text-sm text-gray-500 mt-1">
                    Run Time : {{this.runTime}} Seconds 
                </p>
            </div>
        </div>
        {{/each}}
    </main>
</body>
</html>
